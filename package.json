{
  "name": "onframe",
  "version": "0.3.3",
  "private": true,
  "description": "Extensao local para editar anuncios do Mercado Livre pela pagina do produto.",
  "type": "commonjs",
  "scripts": {
    "start": "node service/server.js",
    "check": "powershell -NoProfile -ExecutionPolicy Bypass -File scripts/bootstrap/check.ps1",
    "package:release": "node scripts/release/package-release.js",
    "release:notes": "node scripts/release/prepare-release-notes.js",
    "version:check": "node scripts/release/check-version.js",
    "version:set": "node scripts/release/set-version.js",
    "audit:deps": "npm audit --audit-level=moderate",
    "release:check": "npm run version:check && npm run audit:deps && npm run test:all && npm run package:release",
    "test": "node --test",
    "test:all": "node --test test/commerce.test.js test/extension.test.js test/items.test.js test/photos.test.js test/service.test.js"
  },
  "engines": {
    "node": ">=20"
  }
}
